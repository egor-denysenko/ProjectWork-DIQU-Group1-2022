version: '3.9'
services:
  redis:
   image: redis:7.0-alpine
   restart: always
   ports:
     - "6379:6379"
  cloud_sender:
    build:
      context: ./cloudSender
    restart: always
    depends_on:
      - "redis"
